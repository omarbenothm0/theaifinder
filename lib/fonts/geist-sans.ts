import { Geist } from 'next/font/google';

/** Geist Sans — body text, paragraphs, descriptions, labels, metadata, and readable text. */
export const geistSans = Geist({
  subsets: ['latin'],
  variable: '--font-geist-sans',
  display: 'swap',
});
